int race() {
    int a = 0 
    int b = 0
    a = a + b 
}

    int exemplo = 0. {id.value = integer ou 0, id.type = typeInt ou int} TYPE ID inserir(exemplo, 0)
    int e = 1.
    //double c = 12,2.
    a = exemplo + e. {id[i].value = id[i-1].value + id[i-2].value}
    

    laps(int i = 0; i<50; i = overtake) {
        exemplo = i.
        prt(exemplo).
    }
    
    @Exemplo de condicional if-else@
    int velocidade = 120,0.
    pitEntry (velocidade > 100) {
        prt("Velocidade alta, reduza éã!").
    } pitExit {
        prt("Velocidade segura.").
    }


   
    int a = 10.
    int b = 5.
    int soma = a + b.
    int subtracao = a - b.
    int multiplicacao = a * b.
    int divisao = a / b.
    str string1 = "opa".

    prt+("Soma: " + soma).
    prt("Subtracao: " + subtracao).
    prt("Multiplicacao: " + multiplicacao).
    prt("Divisao: " + divisao).

    bool x = true.
    bool y = false.
    bool and_op = x && y.
    bool or_op = x || y.
    bool not_op  = !x.

    prt("AND: " + and_op).
    prt("OR: " + or_op).
    prt("NOT: " + not_op).

    prt("NOT: " + not_op).

    @ dasdasdas@
}
