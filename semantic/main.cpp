// pelo que estou entendendo
// precisa puxar a gramatica do syntatic (grammar.cpp)

// cada produção na gramatica deve conter regras semanticas que manipulam esses atributos (ver se vão ser herdados ou sitentizado )

// criar uma tabela de dados com nome e tipo (nos so temos um escopo ent ta suave)